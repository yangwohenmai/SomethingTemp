说说印象最深的项目，学到什么
自己缺点

后端
framwork和metcore
对象引用和值引用的区别
堆和栈的区别？
class和struct的异同
栈是编译期间就分配好的内存空间，因此你的代码中必须就栈的大小有明确的定义；局部值类型变量、值类型参数等都在栈内存中。
堆是程序运行期间动态分配的内存空间，你可以根据程序的运行情况确定要分配的堆内存的大小。

foreach遍历集合时候怎么修改集合的值
StringBuilder 和 String 
字符串是不可变的吗？string s="abc";s="123"不就是变了吗
string str = null 与 string str =””
using关键字有什么用？什么是IDisposable，using结束后会自动调用该对象的Dispose
接口，虚方法，抽象，和类：抽象类不实现，接口里都是抽象类，虚方法可以实现
值类型和引用类型
装箱与拆箱 装箱：从值类型接口转换到引用类型 拆箱：从引用类型转换到值类型。
三层架构 MVC
override与重载（overload

C#中的委托是什么？事件是不是一种委托？事件是不是一种委托。
委托可以把一个方法作为参数代入另一个方法。
委托可以理解为指向一个函数的指针。
是，是一种特殊的委托

托管代码：使用CLR编译语言编辑器开发编写的代码就叫托管代码。
unsafe

接口可以继承接口
CLR垃圾回收机制，不同级别垃圾回收周期
深克隆 / 浅克隆
ref / out：
ref将参数的参数值和引用都传入方法中,所以ref的参数的初始化必须在方法外部,进行,也就是ref的参数必须有初始化值,否则程序会报错
out不会将参数的参数值传入方法中,只会将参数的引用传入方法中,所以参数的初始化工作必须在其对用方法中进行,否则程序会报错
单例模式，懒汉模式（类加载时不初始化）饿汉式单例模式（在类加载时就完成了初始化，所以类加载较慢，但获取对象的速度快）
什么是内存泄漏？
hash怎么用，什么场景，hash冲突是什么
三元运算符是？
反射：要给发射下一个定义还是比较难的，这里先说说我的理解。反射提供了封装程序集，模块和类型对象，可以用反射动态地创建类型的实例，将类型绑定到现有对象，或者从现有对象类型里获取类型，然后调用类型的方法或访问字段和属性。

进程和线程的区别？
答：进程是系统进行资源分配和调度的单位；线程是CPU调度和分派的单位，一个
进程可以有多个线程，这些线程共享这个进程的资源。

网页
用过什么技术
post 和get
AJAX解决什么问题？如何使用AJAX？AJAX有什么问题需要注意？项目中哪里用到了AJAX？
Cookie和 Session 两种会话有什么不同
说一下盒模型
点击发送后，流程
三次握手，四次挥手，为什么必须三次握手，核心是有个互相确认的参数
UDP和TCP连接有和异同




数据库
什么是sql注入？如何避免sql注入
写出一条Sql语句：取出表A中第31到第40记录
数据库为什么会死锁，解决方法
聚集索引 非聚集索引：表记录的排列顺序和索引的排列顺序一致，查询效率快，缺点就是修改慢，非聚集索引层次多，不会造成数据重排，采用B+树结构
索引的作用？和它的优点缺点是什么？缺点是更新数据时效率低，因为要同时更新索引
事务是什么？
什么叫视图？游标是什么？
inner 和 unit left  right
.什么是存储过程？用什么来调用？优点
什么是主键?什么是外键?
悲观锁的特点是先获取锁，再进行业务操作，
乐观锁，也叫乐观并发控制，在提交数据更新之前，每个事务会先检查在该事务读取数据后，有没有其他事务又修改了该数据。如果其他事务有更新的话，那么当前正在提交的事务会进行回滚
乐观锁在不发生取锁失败的情况下开销比悲观锁小
为什么用自增列作为主键：如果使用非自增主键（如果身份证号或学号等），由于每次插入主键的值近似于随机，因此每次新纪录都要被插到现有索引页得中间某个位置，此时MySQL不得不为了将新记录插到合适位置而移动数据
非关系型数据库和关系型数据库区别，优势比较?
最左前缀原则
Hash索引和B+树索引的特点：
Hash索引结构的特殊性，其检索效率非常高，索引的检索可以一次定位;
B+树索引需要从根节点到枝节点，最后才能访问到页节点这样多次的IO访问;
B树和B+树的区别
B树，每个节点都存储key和data，所有节点组成这棵树，并且叶子节点指针为nul，叶子结点不包含任何关键字信息。
B+树，所有的叶子结点中包含了全部关键字的信息，及指向含有这些关键字记录的指针，且叶子结点本身依关键字的大小自小而大的顺序链接，所有的非终端结点可以看成是索引部分，结点中仅含有其子树根结点中最大（或最小）关键字。 (而B 树的非终节点也包含需要查找的有效信息)

红黑树	增加，删除，红黑树会进行频繁的调整，来保证红黑树的性质，浪费时间
B树也就是B-树	B树，查询性能不稳定，查询结果高度不致，每个结点保存指向真实数据的指针，相比B+树每一层每屋存储的元素更多，显得更高一点。
B+树	B+树相比较于另外两种树,显得更矮更宽，查询层次更浅


SqlDataReader 高效，功能弱，只读访问
SqlDataAdapter 强大，要求资源也大一点
SqlDataReader 只能在保持跟数据库连接的状态下才可以读取。。。
SqlDataAdapter 大多情况下是一次性读取一个表，然后填充到DataSet中，然后就可以断开跟数据库的连接了。
